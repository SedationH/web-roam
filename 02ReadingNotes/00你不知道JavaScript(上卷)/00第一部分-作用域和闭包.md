## 作用域是什么

利用变量 表示 状态

怎么找到这些变量 / 状态？(规则) -> 作用域



### 编译原理

JS 需要通过JS引擎进行编译



传统的编译过程有三个步骤

1. 词法分析
   1. 形成编程语言中的最小单位 token
2. 语法分析
   1. 将token 形成 AST语法树🌲
3. 代码生成
   1. 根据AST生成可执行代码



传统的语言的编译过程是在构建之前。

JS的大部分编译情况发生在代码执行前的几微秒。没有提前构建的过程（不是提前编译的），编译结果也不能在分布式系统中进行移植。

> 在我们讨论的作用域背后，JS引擎用尽了各种办法（比如JIT just-in-time，可以延迟编译甚至重施编译）来保证性能最佳。



### 理解作用域

参与者

1. 引擎
   1. 从头到尾负责JS程序的编译与执行过程
   2. 有点像总控程序
2. 编译器
   1. 负责词法分析、语法分析、代码生成
3. 作用域
   1. 负责收集并维护由所有声明标识符（变量）组成的一系列查询，并实施一套非常严格的规则，确定当前执行的代码对这些标识符的访问权限。



`var a = 2`

有两个动作

1. 编译器分析过程中，看当前作用域是否存在a，若无，会在当前作用域中声明a
2. 引擎在执行可执行代码的时候利用作用域查找a



上面三位参与者之间的关系

引擎负责调度编译器和执行代码



编译器在工作过程中完善作用域

引擎执行代码过程中需要作用域提供支持（查找标识符



查找标识符的方式有两种

LHS RHS

> **LHS** is informal shorthand for the **left-hand side** of an [equation](https://en.wikipedia.org/wiki/Equation). Similarly, **RHS** is the **right-hand side**. 

从效果上来看

- LHS是拿到存储变量的容器

- RHS是拿到值



```js
function foo(a) {
  var b = a;
  return a + b;
}

var c = foo(2)
```

LHS

1. c = ..
2. a = .. (隐式)
3. b = ..

RHS

1. foo(2)
2.  = a
3. return a ...
4.  ... + b



### 作用域嵌套

```js
function foo(a) {
  console.log( a+b )
}

var b = 2

foo(2)
```

b 在进行 RHS 的时候需要全局作用域中的标识符b进行支持



foo函数创建作用域

foo声明在全局中

在foo函数中的作用域查询链

foo scope -> global scope



### 异常

**在非严格模式下**

考虑如下代码

```js
function foo(a) {
  console.log( a+b )
  b = a
}

foo(2)
```

对b进行RHS查询的时候、在整个作用域链中都无法找到，会有ReferenceError

`Uncaught ReferenceError: b is not defined`



在对b进行LHS的时候，直到global scope中还未寻到，会自动创建相应的需求标识符 (发现也和作用域表产生了创建的交互，不仅是查询了)



## 词法作用域

作用域目前定义为一个规则

这个规则用于管理引擎在作用域链中如何根据标识符进行变量查找



在JavaScript内部，作用域确实和对象类似，可见的标识符都是它的属性。

但是作用域 “对象” 无法通过JavaScript代码访问，它存在于JavaScript引擎内部。



作用域的工作模式有两种

- 词法作用域
- 动态作用域



### 词法阶段

词法作用域这种命名方式源于编译的第一个过程

词法分析 即从 源代码中拆分token的过程

> 简单的说，词法作用域就是定义在词法阶段的作用域



### 欺骗语法

就是在讨论如何在运行时影响作用域

> JS中有两种机制来实现这个目的，社区普遍认识为不要使用，便利带来的收益不抵性能下降带来的损耗



1. eval

在非严格模式下

`eval(agr: stirng)`

arg表现为好像原来代码就写在那里一样

```js
function foo(evalString) {
  eval(evalString)
  console.log(b)
  b = 'in foo'
  console.log(b)
}
var b = 'global'
foo('var b = "in eval || foo"')
console.log(b)

// in eval || foo
// in foo
// global
```

在严格模式下

```js
function foo(evalString) {
  'use strict'
  eval(evalString)
  console.log(b)
  b = 'in foo'
  console.log(b)
}
var b = 'global'
foo('var b = "in eval || foo"')
console.log(b)

// global
// in foo
// in foo
```

严格模式下 eval有自己的作用域，不会影响外界

2. with

```js
var a
function foo(obj) {
  with (obj) {
    a = 1
  }
}

const o1 = { a: 2 }
const o2 = { b: 2 }

console.log(a, o1.a)
foo(o1)
console.log(a, o1.a)

console.log(a, o2.a)
foo(o2)
console.log(a, o2.a)
// undefined 2
// undefined 1
// undefined undefined
// 1 undefined
```

进行修改

```js
var a
function foo(obj) {
  var a
  with (obj) {
    a = 1
    var bar = 'bar'
    try {
      console.log('in with RHS', b)
    } catch (error) {
      console.log('cant find b when obj is ', JSON.stringify(obj))
    }
  }
  console.log(obj) // {a: 1} || {b: 2}
}

const o1 = { a: 2 }
const o2 = { b: 2 }

console.log(a, o1.a)
foo(o1)
console.log(a, o1.a)

console.log(a, o2.a)
foo(o2)
console.log(a, o2.a)
```



这个例子可以把with的机制看的更加清晰

对于`with`中的代码，在其进行RHS && LHS寻求值、容器的时候，在原有的作用域链上加了一层obj

从这个机制上来看，可以用类似object的方式来实现对作用域的组织

其他的基本原理和边界情况与正常RHS && LHS情况一致



有趣的是，`var bar = 'bar'`是声明语句，但效果上并没有在obj上添加bar，从声明创建的角度来看，应是在当前scope创建标识符，这个过程是在编译阶段完成的，而对于with的效果产生于执行时，没办法，也不应该能在obj上通过var 声明来挂对象

(我感觉自己比书本上说的要清楚😄)



### 性能

为啥用with eval会影响性能？

因为编译器进行的优化大多依赖于词法静态分析



## 函数作用域和块作用域

### 创建作用域的方式

- 函数(普通 箭头函数)
- 
- strict mode 下的 eval
- 全局
  - 多个script共用一个全局，按顺序部分解析+执行
  - 比如在下面scirpt声明的数据(var function) 上面 script重没有变量提升过来

![image-20210725170604947](http://picbed.sedationh.cn/image-20210725170604947.png)

奇了怪了 this 这里是window  能打印出来 debug 台显示不出来

> 从最小特权原则中引申出来的，也叫最小授权或最小暴露原则。这个原则是指在软件设计中，应该最小限度地暴露必要内容，而将其他内容都“隐藏”起来，比如某个模块或对象的 API 设计。 

### 函数表达式

> 区分函数声明和表达式最简单的方法是看 function 关键字出现在声明中的位 置（不仅仅是一行代码，而是整个声明中的位置）。如果 function 是声明 的第一个词，那么就是一个函数声明，否则就是一个函数表达式。 

两种写法

```js
;(function () {})()

;(function () {}())
```

更喜欢前者，把（）搞起来的函数声明 + （）理解为函数表达式返回的引用 -> 再结合()进行调用



>  行内函数表达式非常强大且有用——匿名和具名之间的区别并不会对这点有任何影响。给函 数表达式指定一个函数名可以有效解决以上问题。始终给函数表达式命名是一个最佳实践： 



注意一下undefined

不是保留字 并且在非全局scope才会出现的现象

window.undefined不可更改

The global `undefined` property represents the primitive value `undefined`. It is one of JavaScript's [primitive types](https://developer.mozilla.org/en-US/docs/Glossary/Primitive).



| Property attributes of `undefined` |      |
| :--------------------------------- | ---- |
| Writable                           | no   |
| Enumerable                         | no   |
| Configurable                       | no   |

```js
(function () {
  const undefined = 'foo'
  console.log(undefined) // foo
})()
```



在UMD中的应用

```js
; (function (fn1, fn2) {
  fn1()
  fn2()
})(() => console.log(1), () => console.log(2))
```



### 块作用域

除了 let const 在ES6之前也有

考虑



with

try catch



如何利用try catch 来使用块级作用域？

```js
{
  let a = 1
  console.log(a)
}

console.log(a)
```

```js
try {
  throw 1
} catch (a) {
  console.log(a)
}

console.log(a)
```

> 绝了，还能这么用



这在工具提供ES6兼容支持的时候很好用



IIEF也能搞，但两者是不同的作用域

> 其次，IIFE 和 try/catch 并不是完全等价的，因为如果将一段代码中的任意一部分拿出来用函数进行包裹，会改变这段代码的含义其中的 this、return、break 和 contine 都会 发生变化。IIFE 并不是一个普适的解决方案，它只适合在某些情况下进行手动操作。 

### 提升

编译 -> 提升

在当前(函数)作用域下

函数优先于变量

function > var



考虑下面转换

```js
foo(); // TypeError 
bar(); // ReferenceError
var foo = function bar() {

};
```

```js
var foo

foo()
bar()
foo = function () {
  var bar = arguments.callee
}
```



### 作用域和闭包

定义闭包

当函数可以记住并访问所在的词法作用域时，就产生了闭包。

特指函数是在当前词法作用域之外执行。 

```js
// 核心在于对引用的解决
for (var i = 0; i < 3; i++) {
  setTimeout(() => {
    console.log(i) // 3 3 3
  }, 0)
}

for (var i = 0; i < 3; i++) {
  ;(function (i) {
    setTimeout(() => {
      console.log(i)
    }, 0)
  })(i)
}
```





模块组织方案中大量运用闭包，理解下面代码中的逻辑

```js
var MyModules = (function Manager() {
  var modules = {}
  function define(name, deps, impl) {
    for (var i = 0; i < deps.length; i++) {
      // 必须已经存在在modules的依赖才可以
      deps[i] = modules[deps[i]]
    }
    // 关键
    modules[name] = impl.apply(impl, deps)
  }
  function get(name) {
    return modules[name]
  }
  return { define: define, get: get }
})()

MyModules.define('bar', [], function () {
  function hello(who) {
    return 'Let me introduce: ' + who
  }
  return { hello: hello }
})
MyModules.define('foo', ['bar'], function (bar) {
  var hungry = 'hippo'
  function awesome() {
    return bar.hello(hungry).toUpperCase()
  }
  return { awesome: awesome }
})

var bar = MyModules.get('bar')
var foo = MyModules.get('foo')

console.log(bar.hello('hippo')) // Let me introduce: hippo
console.log(foo.awesome()) // LET ME INTRODUCE: HIPPO
```



## 动态作用域

作用域是指执行引擎如何根据标识符寻找变量



词法指根据词法分析的结果来拿到变量值

```js
var a = 2

function foo() {
  console.log(a) // 2
}
function bar() {
  var a = 3
  foo()
}

bar()
```



动态作用域 动态指根据函数调用栈来根据标识符寻找变量

和上面一样的实例，如果是动态

```js
var a = 2

function foo() {
  console.log(a) // 3
}
function bar() {
  var a = 3
  foo()
}

bar()
```



词法作用域关注函数在哪里声明

动态作用域关注函数从何处调用



JS的引擎执行是根据词法的，this的机制很像动态的





## this 词法

```js
const foo = {
  name: 'foo',
  getName: function () {
    console.log(this.name) // foo
  },
}

foo.getName()

```

```js
this.name = 'global'

const foo = {
  name: 'foo',
  getName: () => {
    console.log(this)
  },
}

foo.getName()

```

```js
<script>
  globalThis.name = 'global'

  const foo = {
    name: 'foo',
    getName: function () {
      setTimeout(function () {
        console.log(this.name) // global
      }, 0)
    },
  }

  foo.getName()
</script>
```

```js
<script>
  globalThis.name = 'global'

  const foo = {
    name: 'foo',
    getName: function () {
      setTimeout(() => {
        console.log(this.name) // global
      }, 0)
    },
  }

  foo.getName()

</script>
```

